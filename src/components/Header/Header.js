import React from 'react';
import './Header.css';
import '../../index.css';

const header = () => {
    return(
        <header>
            <h2>sweeper-78</h2>
            <button>back</button>
        </header>
    )
}

export default header;
